workflows:
  android-build:
    name: Build APK
    environment:
      android: true
      java: 17
    scripts:
      - name: Make gradlew executable
        script: chmod +x ./gradlew
      - name: Build Debug APK
        script: ./gradlew :assembleDebug
    artifacts:
      - build/**/*.apk

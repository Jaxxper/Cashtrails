workflows:
  android-build:
    name: Build Android App
    environment:
      android:
        image: android-31
    scripts:
      - ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
